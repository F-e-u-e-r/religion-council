# macOS
.DS_Store

# Python (scripts/ and the RAG roadmap)
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
.venv/
venv/
env/
.ipynb_checkpoints/

# Local env / secrets (RAG server, embeddings keys)
.env
.env.*
*.local

# Vector store / build artifacts (added in later roadmap phases)
/index/
/.cache/
/.religion-council/

# Per-user agent settings (shared .mcp.json remains tracked)
/.claude/settings.local.json
/.claude/*.local.json
/.codex/

# Editors
.idea/
.vscode/
*.swp
*~
